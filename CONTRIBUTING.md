# Contributing

## Development

1. Run `make setup`
2. Run `make dev` for local development
3. Run `make test && make lint` before opening updates

## Commit style

- Keep PRs focused and small.
- Add/update tests with behavioral changes.
